一 脚本第一行 #!/bin/sh(#!/bin/bash) 使用最多
1、#!/bin/csh 说明是csh脚本
2、#!/bin/awk 说明是awk程序

二 基本元素（执行命令）
1、多个命令顺序执行，要以;分隔开
2、命令在后台执行使用&符号
3、变量定义
name=syx (注意变量和等号之间不能有空格)
使用变量 $name

三 重定向和管道
1、<
2、> , >>(表示追加)
3、| 管道

